# Задачи — выполнять по порядку

Отмечайте чекбоксы по мере выполнения. Параллельно держите под рукой [SERVICES_SETUP.md](./SERVICES_SETUP.md) (что копировать в `.env`).

**Как вызывать Cursor по одному шагу (промпты, CI, merge):** [AI_PLAYBOOK.md](./AI_PLAYBOOK.md).

## Фаза 0 — Окружение репозитория

- [x] **T0.1** Прочитать [ARCHITECTURE.md](./ARCHITECTURE.md) и этот файл.
- [x] **T0.2** Скопировать `.env.example` → `.env` (файл `.env` не коммитить).
- [x] **T0.3** При необходимости создать каталог `local/` под черновики (см. [LOCAL_WORKFLOW.md](./LOCAL_WORKFLOW.md)).

## Фаза 1 — Сервисы и секреты

- [x] **T1.1** RetailCRM: URL демо-аккаунта и **API-ключ** (не пароль от витрины для интеграции) — см. SERVICES_SETUP.
- [x] **T1.2** Supabase: проект, URL, **anon** и **service_role** ключи; создать таблицы (SQL из будущей миграции или вручную в SQL Editor).
- [x] **T1.3** Vercel: аккаунт Hobby, проект связан с GitHub-репо (после появления кода).
- [ ] **T1.4** Telegram: токен бота от BotFather, **chat_id** личного чата или группы для уведомлений (настройка есть; нужно подтвердить доставку реального сообщения).

## Фаза 2 — RetailCRM и данные

- [x] **T2.1** Реализовать скрипт загрузки заказов из `mock_orders.json` в RetailCRM через API (`npm run upload:retailcrm`, см. [RETAILCRM_UPLOAD.md](./RETAILCRM_UPLOAD.md)).
- [x] **T2.2** Проверить через API и интерфейс RetailCRM, что заказы отображаются, суммы корректны (загрузка `gbc-mock-001..050` успешна).

## Фаза 3 — Синхронизация в Supabase

- [x] **T3.1** Реализовать скрипт «выгрузка заказов из RetailCRM → upsert в Supabase» (`npm run sync:supabase`, схема в `docs/supabase-schema.sql`).
- [x] **T3.2** Идемпотентность: используется upsert по `external_id` (повторный запуск обновляет строки без дублей).

## Фаза 4 — Дашборд

- [x] **T4.1** Инициализировать Next.js (App Router + TypeScript) в репозитории.
- [x] **T4.2** Страница с графиком по дням из данных Supabase (`/`, API: `/api/orders/series`).
- [x] **T4.3** Сборка `npm run build` без ошибок; запуск описан в README.

## Фаза 5 — Telegram

- [x] **T5.1** Серверная логика: обнаружение заказа с суммой **> 50 000 ₸** (в `sync:supabase`, поле `total_kzt`).
- [x] **T5.2** Отправка сообщения в Telegram; защита от повторных уведомлений по одному заказу (`order_events`, уникальность `external_id + event_type`).
- [ ] **T5.3** Скриншот уведомления для сдачи.

## Фаза 6 — Деплой и сдача

- [x] **T6.1** Подключить репо к Vercel, задать env vars, получить URL продакшена.
- [x] **T6.2** Настроить Vercel Cron (через `vercel.json`) для синка/проверки новых заказов (`/api/cron/sync`).
- [ ] **T6.3** Обновить корневой `README.md`: промпты AI, блокеры, решения; ссылки на Vercel и репо.
- [ ] **T6.4** Отправить результат в Telegram @DmitriyKrasnikov по инструкции из ТЗ.